dist
node_modules
bun.lock
CHANGELOG.md

# Regenerated by mcp-publisher; let it own the formatting.
server.json

# Published verbatim by `standup instructions` — reformatting changes what
# users receive.
skills

# Release and CI definitions: reindenting them is churn on the one path that
# must not break.
.github
